{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Rekan Setia Dev",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-Command",
        "taskkill /F /IM node.exe 2>$null | Out-Null; if (Test-Path .next) { Remove-Item -Recurse -Force .next }; npm run dev"
      ],
      "problemMatcher": []
    }
  ]
}
